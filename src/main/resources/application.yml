logging:
  level:
    org:
      springframework:
        data:
          r2dbc: DEBUG

spring:
  r2dbc:
    url: r2dbc:pool:mysql://127.0.0.1:3306/testdb
    name: sa
    password:
  pool:
    initial-size: 100
    max-size: 500
    max-idle-time: 30m
    validation-query: SELECT 1

webclientexample:
  #  postsapi:
  #    host: https://localhost
  h2-console-port: 8081
